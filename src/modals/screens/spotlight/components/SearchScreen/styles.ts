import { StackProps } from '@chakra-ui/react';

/**
 * @constant {StackProps} $stackStyles - Style properties for the Stack component in the search box
 */

export const $stackStyles: StackProps = {
  justifyContent: 'space-between',
  pb: 3,
};
