import { StackProps, HeadingProps } from '@chakra-ui/react';

export const $stackStyles: StackProps = {
  spacing: 6,
  width: '100%',
  mt: 12,
};

export const $headingStyles: HeadingProps = {
  size: 'xl',
  color: 'white',
};
