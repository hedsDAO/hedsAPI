import { TextProps } from '@chakra-ui/react';

/**
 * @constant {TextProps} $minimizeButtonStyles
 * @description TextProps object that contains styles for the MinimizeButton component.
 */

export const $minimizeButtonStyles: TextProps = {
  pointerEvents: 'auto',
  role: 'button',
  fontSize: 'xs',
  right: 7,
  top: 1,
  className: 'fa-solid fa-window-minimize',
  position: 'absolute',
  color: 'white',
  opacity: '75%',
};
