import { SettingsModelState } from '@/modals/screens/settings/models/common';
import { user } from '@/tests/mocks/data/user';

export const settingsModelState = {
  userData: user,
} as SettingsModelState;
